import { View, Text, StyleSheet } from 'react-native'

import { MEALS } from '../../data/dummy-data'

function MealsOverviewTest() {
	return (
		<View style={styles.container}>
			<Text>tests..................</Text>
		</View>
	)
}

export default MealsOverviewTest

const styles = StyleSheet.create({
	container: {
		flex: 1,
		padding: 16
	}
})

